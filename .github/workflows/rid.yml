name: counsel

on:
  push:

jobs:
  keen:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        apply: ["portion", "define", "rank", "cheek", "mutter"]
        republic: ["absence", "file", "cherry", "acceptable"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java --enable-native-access=ALL-UNNAMED -jar An.jar
